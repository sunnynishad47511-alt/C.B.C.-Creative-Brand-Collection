import { Routes, Route, useLocation } from 'react-router-dom'
import { AnimatePresence, motion } from 'framer-motion'
import Navbar from './components/Navbar'
import Home from './pages/Home'
import Shop from './pages/Shop'
import Quiz from './pages/Quiz'
import VirtualCloset from './pages/VirtualCloset'
import Product from './pages/Product'
import Checkout from './pages/Checkout'

function AppContent() {
  const location = useLocation()
  
  return (
    <div className="min-h-screen bg-cyber-black">
      <Navbar />
      <AnimatePresence mode="wait">
        <Routes location={location} key={location.pathname}>
          <Route path="/" element={<Home />} />
          <Route path="/shop" element={<Shop />} />
          <Route path="/shop/:id" element={<Product />} />
          <Route path="/quiz" element={<Quiz />} />
          <Route path="/virtual-closet" element={<VirtualCloset />} />
          <Route path="/checkout" element={<Checkout />} />
        </Routes>
      </AnimatePresence>
      <footer className="py-12 text-center text-gray-500 border-t border-neon-green/20 mt-24">
        <p>&copy; 2024 C.B.C. Creative Brand Collection. All rights reserved.</p>
      </footer>
    </div>
  )
}

export default function App() {
  return (
    <AppContent />
  )
}